#include<stdio.h>
void main()
{
	float a, b, c;
	float slope;
	
	
	printf("enter the no:");
	 scanf("%f %f %f", &a, &b, &c);
        	 
	slope = -a/b;
	printf("the slope is:%f", slope);
}
